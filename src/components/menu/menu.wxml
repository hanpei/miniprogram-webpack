<view class="menu-container" scroll-x="{{true}}">
  <view
    class="item {{currentIndex === index && 'current'}}"
    wx:for="{{items}}"
    wx:key="index"
    bindtap="onTap"
    data-index="{{index}}"
  >
    <text>{{item}}</text>
  </view>
</view>
